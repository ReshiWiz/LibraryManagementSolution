@model LibraryManagement.Models.Reports

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Reports</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TransId" class="control-label"></label>
                <select asp-for="TransId" class ="form-control" asp-items="ViewBag.TransId"></select>
            </div>
            <div class="form-group">
                <label asp-for="BorrowingId" class="control-label"></label>
                <select asp-for="BorrowingId" class ="form-control" asp-items="ViewBag.BorrowingId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ReportDate" class="control-label"></label>
                <input asp-for="ReportDate" class="form-control" />
                <span asp-validation-for="ReportDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
